x-database-environment: &database-environment
  SDKWORK_DATABASE_ENGINE: postgresql
  SDKWORK_DATABASE_HOST: postgres
  SDKWORK_DATABASE_PORT: "5432"
  SDKWORK_DATABASE_NAME: sdkwork_ai_dev
  SDKWORK_DATABASE_SCHEMA: sdkwork_ai_dev
  SDKWORK_DATABASE_SCHEMA_FALLBACK_PUBLIC: "false"
  SDKWORK_DATABASE_USERNAME: sdkwork_ai_dev
  SDKWORK_DATABASE_PASSWORD: sdkworkdev123
  SDKWORK_DATABASE_SSL_MODE: disable
  SDKWORK_DATABASE_MAX_CONNECTIONS: "10"

services:
  forum-api:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile.server
    ports:
      - "8080:8080"
    environment:
      <<: *database-environment
      RUST_LOG: info
      SDKWORK_FORUM_SEARCH_URL: http://search:9200
      SDKWORK_FORUM_DRIVE_URL: http://drive:8080
    depends_on:
      - postgres
      - search

  forum-worker:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile.worker
    environment:
      <<: *database-environment
      RUST_LOG: info
      SDKWORK_FORUM_SEARCH_URL: http://search:9200
      SDKWORK_FORUM_DRIVE_URL: http://drive:8080
    depends_on:
      - postgres
      - search

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: sdkwork_ai_dev
      POSTGRES_USER: sdkwork_ai_dev
      POSTGRES_PASSWORD: sdkworkdev123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init/001-workspace-schema.sql:/docker-entrypoint-initdb.d/001-workspace-schema.sql:ro

  search:
    image: opensearchproject/opensearch:2
    environment:
      discovery.type: single-node
      plugins.security.disabled: "true"
    ports:
      - "9200:9200"
    volumes:
      - search_data:/usr/share/opensearch/data

volumes:
  postgres_data:
  search_data:
